import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

// Создаем корневой элемент для приложения, используя метод createRoot из ReactDOM.
ReactDOM.createRoot(document.getElementById("root")!).render(<App />);
